// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://github-page-test.github.io/',
  // base: '/github-page-test/'
});
